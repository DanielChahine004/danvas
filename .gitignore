# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/
env/

# Python build output (root only — the committed frontend bundle at
# danvas/frontend/dist/ is intentionally kept and must NOT be ignored).
/build/
/dist/

# Frontend toolchain (the built bundle is committed; its deps are not)
node_modules/
danvas/frontend/.vite/

# Generated example output (created by running the save/load + upload examples)
examples/saved_canvas.json
examples/saved_layout.json
examples/uploads/

# Auto-persistence files written by serve(persist=...) — runtime canvas state,
# not source (the default name is <script>.canvas.json next to the script).
*.canvas.json

# Live state written by the hackathon example (catalogue, teams, orders,
# announcement board) — runtime data, not source.
examples/hackathon/hackathon_inventory.csv
examples/hackathon/hackathon_teams.csv
examples/hackathon/hackathon_orders.csv
examples/hackathon/hackathon_announcement.md

# Editor / OS / local agent state
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
claude.ps1
.mcp.json
# Added by code-review-graph
.code-review-graph/
.codebaser/
examples/test.py
