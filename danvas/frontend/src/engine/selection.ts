// Superseded by engine/interaction.ts (which unifies selection, marquee,
// multi-move, draw-tool creation, and keyboard). Kept as a re-export so any
// stale import still resolves.
export { attachInteraction as attachPanelGestures } from './interaction'
